# Core LangChain (unpinned upper bound so pip can resolve wheels that support Python 3.14)
langchain>=0.3.0
langchain-core>=0.3.0
langchain-groq>=0.2.0

# Env variable / API key loading
python-dotenv>=1.0.0
